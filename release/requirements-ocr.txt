# PaddleOCR FastAPI 服務依賴（api/paddleocr_service.py）
# 安裝：pip install -r requirements-ocr.txt
# 建議在專案根目錄或獨立 venv 使用

fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6
# Pin to stable CPU combo to avoid PIR/oneDNN runtime regression in some environments
paddleocr==2.7.3
paddlepaddle==2.6.2
Pillow>=9.0.0
# Keep NumPy 1.x for OpenCV/PaddleOCR ABI compatibility in this stack
numpy<2

# Unified backend options (auto/openvino/onnx)
rapidocr_onnxruntime>=1.4.4
onnxruntime-openvino>=1.23.0
